# this script categozies the plans with high similarity

import argparse
import json

parser = argparse.ArgumentParser()
parser.add_argument('plans-dir', help='path to the plans directory in JSON')
parser.add_argument('--output-dir', help='directory whwere the JSON is put in', required=True)

plan_list = []

def plan_load():

def plans_comparision(plan1,plan2):


def print_results():
	